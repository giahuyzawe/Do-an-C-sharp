<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="FoodStreetGuide.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:FoodStreetGuide"
    Title="FoodStreetGuide"
    Shell.BackgroundColor="{StaticResource Surface}"
    Shell.ForegroundColor="{StaticResource TextPrimary}"
    Shell.TitleColor="{StaticResource TextPrimary}"
    Shell.TabBarBackgroundColor="{StaticResource Surface}"
    Shell.TabBarForegroundColor="{StaticResource Primary}"
    Shell.TabBarTitleColor="{StaticResource Primary}"
    Shell.TabBarUnselectedColor="{StaticResource TextSecondary}">

    <TabBar>
        <ShellContent
            Title="Map"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Discover"
            ContentTemplate="{DataTemplate local:DiscoverPage}"
            Route="DiscoverPage" />

        <ShellContent
            Title="Saved"
            ContentTemplate="{DataTemplate local:SavedPage}"
            Route="SavedPage" />

        <ShellContent
            Title="Settings"
            ContentTemplate="{DataTemplate local:SettingsPage}"
            Route="SettingsPage" />
    </TabBar>

</Shell>
